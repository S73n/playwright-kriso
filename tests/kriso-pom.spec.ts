/**
 * Part II — Page Object Model tests
 * Refactor your Part I tests to use the page classes in pages/.
 * Page objects are injected via fixtures — no manual instantiation needed.
 *
 * Rules:
 *   - No raw selectors in test files — all locators live in page classes
 *   - Use only: getByRole, getByText, getByPlaceholder, getByLabel
 */
import { test } from './fixtures';
import { expect } from '@playwright/test';

// ─── SEARCH FOR BOOKS BY KEYWORDS ────────────────────────────────────────────

test.describe('Search for Books by Keywords (POM)', () => {

  // TODO: implement tests

});

// ─── ADD BOOKS TO SHOPPING CART ──────────────────────────────────────────────

test.describe('Add Books to Shopping Cart (POM)', () => {

  // TODO: implement tests

});

// ─── NAVIGATE PRODUCTS VIA FILTERS ───────────────────────────────────────────

test.describe('Navigate Products via Filters (POM)', () => {

  // TODO: implement tests

});
